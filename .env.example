# Villages Events Calendar Sync - Environment Variables
# Copy this file to .env for local development

# DynamoDB Configuration
# The name of the DynamoDB table where events will be stored
# For local development, this can be any name you choose
# For AWS deployment, this is set automatically by SAM template
TABLE_NAME=villages-events-local

# Logging Configuration
# Set the logging level for the application
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Use DEBUG for detailed logs during development, INFO for production
LOG_LEVEL=INFO

# Calendar Scraper Configuration
# Number of days ahead to fetch events from The Villages calendar
# Default: 90 (approximately 3 months)
# Increase for longer-term event planning, decrease for faster sync
DAYS_AHEAD=90

# HTTP Request Configuration
# Timeout in seconds for HTTP requests to The Villages calendar website
# Default: 30 seconds
# Increase if experiencing timeout errors, decrease for faster failure detection
TIMEOUT_SECONDS=30

# AWS Configuration (for local development only)
# These are only needed when testing locally with AWS services
# In Lambda, these are provided automatically by the execution environment
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=test
# AWS_SECRET_ACCESS_KEY=test
# AWS_ENDPOINT_URL=http://localhost:8000  # For DynamoDB Local or LocalStack
